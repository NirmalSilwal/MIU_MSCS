package lesson9;

/*
 * @author Nirmal Silwal
 * @contact nsilwal@miu.edu
*/

public class NestedClass {

	/*
	 * applicable in inner class
	 * 
	 * types of nested class - member, static, local, anonymous
	 * 
	 * applications - 1. sort the collecitons using Coparator interface 2. in GUI
	 * applications
	 * 
	 * static methods inside innner class cannot access non static instance fields,
	 * it can only access the static members of the outer class
	 * 
	 * 
	 * lambda - (argument) -> {Body};
	 */
}
