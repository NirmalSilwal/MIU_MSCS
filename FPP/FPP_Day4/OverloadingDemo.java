package lesson4;

/*
 * @author Nirmal Silwal
 * @contact nsilwal@miu.edu
*/

public class OverloadingDemo {

	/*
	 * same method with different signature
	 * 
	 * different signature - number of arguments, argument type change
	 * 
	 * overloading verified at compile time - eg of complie time polymorphism or
	 * early binding
	 * 
	 * return type determined at runtime
	 * 
	 * variation difference after the method name
	 * 
	 * compiler verifies the signature of the method
	 */
}
